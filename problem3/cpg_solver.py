
"""
Auto-generated by CVXPYgen on January 30, 2024 at 06:19:45.
Content: Custom solve method for CVXPY interface.
"""

import time
import warnings
import numpy as np
from cvxpy.reductions import Solution
from cvxpy.problems.problem import SolverStats
from ..problem3 import cpg_module


standard_settings_names = {"max_iters": "maxit"}


def cpg_solve(prob, updated_params=None, **kwargs):

    # set flags for updated parameters
    upd = cpg_module.cpg_updated()
    if updated_params is None:
        updated_params = ["q", "r_0", "v_0", "m_0", "t_f_A", "t_f_B", "t_f_B_g", "alpha", "theta_cos", "n_hat", "v_max", "c", "c_q", "rho_2_exp_z_0", "rho_2_exp_z_0_z_0", "rho_1_exp_z_0", "rho_1_exp_z_0_z_0"]
    for p in updated_params:
        try:
            setattr(upd, p, True)
        except AttributeError:
            raise AttributeError(f"{p} is not a parameter.")

    # set solver settings
    cpg_module.set_solver_default_settings()
    for key, value in kwargs.items():
        try:
            eval(f'cpg_module.set_solver_{standard_settings_names.get(key, key)}(value)')
        except AttributeError:
            raise AttributeError(f'Solver setting "{key}" not available.')

    # set parameter values
    par = cpg_module.cpg_params()
    param_dict = prob.param_dict
    par.q = list(param_dict["q"].value.flatten(order="F"))
    par.r_0 = list(param_dict["r_0"].value.flatten(order="F"))
    par.v_0 = list(param_dict["v_0"].value.flatten(order="F"))
    par.m_0 = param_dict["m_0"].value
    par.t_f_A = list(param_dict["t_f_A"].value.flatten(order="F"))
    par.t_f_B = list(param_dict["t_f_B"].value.flatten(order="F"))
    par.t_f_B_g = list(param_dict["t_f_B_g"].value.flatten(order="F"))
    par.alpha = param_dict["alpha"].value
    par.theta_cos = param_dict["theta_cos"].value
    par.n_hat = list(param_dict["n_hat"].value.flatten(order="F"))
    par.v_max = param_dict["v_max"].value
    par.c = list(param_dict["c"].value.flatten(order="F"))
    par.c_q = list(param_dict["c_q"].value.flatten(order="F"))
    par.rho_2_exp_z_0 = list(param_dict["rho_2_exp_z_0"].value.flatten(order="F"))
    par.rho_2_exp_z_0_z_0 = list(param_dict["rho_2_exp_z_0_z_0"].value.flatten(order="F"))
    par.rho_1_exp_z_0 = list(param_dict["rho_1_exp_z_0"].value.flatten(order="F"))
    par.rho_1_exp_z_0_z_0 = list(param_dict["rho_1_exp_z_0_z_0"].value.flatten(order="F"))

    # solve
    t0 = time.time()
    res = cpg_module.solve(upd, par)
    t1 = time.time()

    # store solution in problem object
    prob._clear_solution()
    prob.var_dict['x'].save_value(np.array(res.cpg_prim.x).reshape((6, 120), order='F'))
    prob.var_dict['sigma'].save_value(np.array(res.cpg_prim.sigma).reshape((1, 120), order='F'))
    prob.var_dict['u'].save_value(np.array(res.cpg_prim.u).reshape((3, 120), order='F'))
    prob.var_dict['z'].save_value(np.array(res.cpg_prim.z).reshape((1, 120), order='F'))
    prob.constraints[0].save_dual_value(np.array(res.cpg_dual.d0).reshape(3))
    prob.constraints[1].save_dual_value(np.array(res.cpg_dual.d1).reshape(3))
    prob.constraints[2].save_dual_value(np.array(res.cpg_dual.d2))
    prob.constraints[3].save_dual_value(np.array(res.cpg_dual.d3).reshape(3))
    prob.constraints[4].save_dual_value(np.array(res.cpg_dual.d4).reshape(1))
    prob.constraints[5].save_dual_value(np.array(res.cpg_dual.d5).reshape(3))
    prob.constraints[6].save_dual_value(np.array(res.cpg_dual.d6).reshape(3))
    prob.constraints[7].save_dual_value(np.array(res.cpg_dual.d7).reshape(120))
    prob.constraints[8].save_dual_value(np.array(res.cpg_dual.d8).reshape((6, 3), order='F'))
    prob.constraints[9].save_dual_value(np.array(res.cpg_dual.d9).reshape(1))
    prob.constraints[10].save_dual_value(np.array(res.cpg_dual.d10).reshape((6, 3), order='F'))
    prob.constraints[11].save_dual_value(np.array(res.cpg_dual.d11).reshape(1))
    prob.constraints[12].save_dual_value(np.array(res.cpg_dual.d12).reshape((6, 3), order='F'))
    prob.constraints[13].save_dual_value(np.array(res.cpg_dual.d13).reshape(1))
    prob.constraints[14].save_dual_value(np.array(res.cpg_dual.d14).reshape((6, 3), order='F'))
    prob.constraints[15].save_dual_value(np.array(res.cpg_dual.d15).reshape(1))
    prob.constraints[16].save_dual_value(np.array(res.cpg_dual.d16).reshape((6, 3), order='F'))
    prob.constraints[17].save_dual_value(np.array(res.cpg_dual.d17).reshape(1))
    prob.constraints[18].save_dual_value(np.array(res.cpg_dual.d18).reshape((6, 3), order='F'))
    prob.constraints[19].save_dual_value(np.array(res.cpg_dual.d19).reshape(1))
    prob.constraints[20].save_dual_value(np.array(res.cpg_dual.d20).reshape((6, 3), order='F'))
    prob.constraints[21].save_dual_value(np.array(res.cpg_dual.d21).reshape(1))
    prob.constraints[22].save_dual_value(np.array(res.cpg_dual.d22).reshape((6, 3), order='F'))
    prob.constraints[23].save_dual_value(np.array(res.cpg_dual.d23).reshape(1))
    prob.constraints[24].save_dual_value(np.array(res.cpg_dual.d24).reshape((6, 3), order='F'))
    prob.constraints[25].save_dual_value(np.array(res.cpg_dual.d25).reshape(1))
    prob.constraints[26].save_dual_value(np.array(res.cpg_dual.d26).reshape((6, 3), order='F'))
    prob.constraints[27].save_dual_value(np.array(res.cpg_dual.d27).reshape(1))
    prob.constraints[28].save_dual_value(np.array(res.cpg_dual.d28).reshape((6, 3), order='F'))
    prob.constraints[29].save_dual_value(np.array(res.cpg_dual.d29).reshape(1))
    prob.constraints[30].save_dual_value(np.array(res.cpg_dual.d30).reshape((6, 3), order='F'))
    prob.constraints[31].save_dual_value(np.array(res.cpg_dual.d31).reshape(1))
    prob.constraints[32].save_dual_value(np.array(res.cpg_dual.d32).reshape((6, 3), order='F'))
    prob.constraints[33].save_dual_value(np.array(res.cpg_dual.d33).reshape(1))
    prob.constraints[34].save_dual_value(np.array(res.cpg_dual.d34).reshape((6, 3), order='F'))
    prob.constraints[35].save_dual_value(np.array(res.cpg_dual.d35).reshape(1))
    prob.constraints[36].save_dual_value(np.array(res.cpg_dual.d36).reshape((6, 3), order='F'))
    prob.constraints[37].save_dual_value(np.array(res.cpg_dual.d37).reshape(1))
    prob.constraints[38].save_dual_value(np.array(res.cpg_dual.d38).reshape((6, 3), order='F'))
    prob.constraints[39].save_dual_value(np.array(res.cpg_dual.d39).reshape(1))
    prob.constraints[40].save_dual_value(np.array(res.cpg_dual.d40).reshape((6, 3), order='F'))
    prob.constraints[41].save_dual_value(np.array(res.cpg_dual.d41).reshape(1))
    prob.constraints[42].save_dual_value(np.array(res.cpg_dual.d42).reshape((6, 3), order='F'))
    prob.constraints[43].save_dual_value(np.array(res.cpg_dual.d43).reshape(1))
    prob.constraints[44].save_dual_value(np.array(res.cpg_dual.d44).reshape((6, 3), order='F'))
    prob.constraints[45].save_dual_value(np.array(res.cpg_dual.d45).reshape(1))
    prob.constraints[46].save_dual_value(np.array(res.cpg_dual.d46).reshape((6, 3), order='F'))
    prob.constraints[47].save_dual_value(np.array(res.cpg_dual.d47).reshape(1))
    prob.constraints[48].save_dual_value(np.array(res.cpg_dual.d48).reshape((6, 3), order='F'))
    prob.constraints[49].save_dual_value(np.array(res.cpg_dual.d49).reshape(1))
    prob.constraints[50].save_dual_value(np.array(res.cpg_dual.d50).reshape((6, 3), order='F'))
    prob.constraints[51].save_dual_value(np.array(res.cpg_dual.d51).reshape(1))
    prob.constraints[52].save_dual_value(np.array(res.cpg_dual.d52).reshape((6, 3), order='F'))
    prob.constraints[53].save_dual_value(np.array(res.cpg_dual.d53).reshape(1))
    prob.constraints[54].save_dual_value(np.array(res.cpg_dual.d54).reshape((6, 3), order='F'))
    prob.constraints[55].save_dual_value(np.array(res.cpg_dual.d55).reshape(1))
    prob.constraints[56].save_dual_value(np.array(res.cpg_dual.d56).reshape((6, 3), order='F'))
    prob.constraints[57].save_dual_value(np.array(res.cpg_dual.d57).reshape(1))
    prob.constraints[58].save_dual_value(np.array(res.cpg_dual.d58).reshape((6, 3), order='F'))
    prob.constraints[59].save_dual_value(np.array(res.cpg_dual.d59).reshape(1))
    prob.constraints[60].save_dual_value(np.array(res.cpg_dual.d60).reshape((6, 3), order='F'))
    prob.constraints[61].save_dual_value(np.array(res.cpg_dual.d61).reshape(1))
    prob.constraints[62].save_dual_value(np.array(res.cpg_dual.d62).reshape((6, 3), order='F'))
    prob.constraints[63].save_dual_value(np.array(res.cpg_dual.d63).reshape(1))
    prob.constraints[64].save_dual_value(np.array(res.cpg_dual.d64).reshape((6, 3), order='F'))
    prob.constraints[65].save_dual_value(np.array(res.cpg_dual.d65).reshape(1))
    prob.constraints[66].save_dual_value(np.array(res.cpg_dual.d66).reshape((6, 3), order='F'))
    prob.constraints[67].save_dual_value(np.array(res.cpg_dual.d67).reshape(1))
    prob.constraints[68].save_dual_value(np.array(res.cpg_dual.d68).reshape((6, 3), order='F'))
    prob.constraints[69].save_dual_value(np.array(res.cpg_dual.d69).reshape(1))
    prob.constraints[70].save_dual_value(np.array(res.cpg_dual.d70).reshape((6, 3), order='F'))
    prob.constraints[71].save_dual_value(np.array(res.cpg_dual.d71).reshape(1))
    prob.constraints[72].save_dual_value(np.array(res.cpg_dual.d72).reshape((6, 3), order='F'))
    prob.constraints[73].save_dual_value(np.array(res.cpg_dual.d73).reshape(1))
    prob.constraints[74].save_dual_value(np.array(res.cpg_dual.d74).reshape((6, 3), order='F'))
    prob.constraints[75].save_dual_value(np.array(res.cpg_dual.d75).reshape(1))
    prob.constraints[76].save_dual_value(np.array(res.cpg_dual.d76).reshape((6, 3), order='F'))
    prob.constraints[77].save_dual_value(np.array(res.cpg_dual.d77).reshape(1))
    prob.constraints[78].save_dual_value(np.array(res.cpg_dual.d78).reshape((6, 3), order='F'))
    prob.constraints[79].save_dual_value(np.array(res.cpg_dual.d79).reshape(1))
    prob.constraints[80].save_dual_value(np.array(res.cpg_dual.d80).reshape((6, 3), order='F'))
    prob.constraints[81].save_dual_value(np.array(res.cpg_dual.d81).reshape(1))
    prob.constraints[82].save_dual_value(np.array(res.cpg_dual.d82).reshape((6, 3), order='F'))
    prob.constraints[83].save_dual_value(np.array(res.cpg_dual.d83).reshape(1))
    prob.constraints[84].save_dual_value(np.array(res.cpg_dual.d84).reshape((6, 3), order='F'))
    prob.constraints[85].save_dual_value(np.array(res.cpg_dual.d85).reshape(1))
    prob.constraints[86].save_dual_value(np.array(res.cpg_dual.d86).reshape((6, 3), order='F'))
    prob.constraints[87].save_dual_value(np.array(res.cpg_dual.d87).reshape(1))
    prob.constraints[88].save_dual_value(np.array(res.cpg_dual.d88).reshape((6, 3), order='F'))
    prob.constraints[89].save_dual_value(np.array(res.cpg_dual.d89).reshape(1))
    prob.constraints[90].save_dual_value(np.array(res.cpg_dual.d90).reshape((6, 3), order='F'))
    prob.constraints[91].save_dual_value(np.array(res.cpg_dual.d91).reshape(1))
    prob.constraints[92].save_dual_value(np.array(res.cpg_dual.d92).reshape((6, 3), order='F'))
    prob.constraints[93].save_dual_value(np.array(res.cpg_dual.d93).reshape(1))
    prob.constraints[94].save_dual_value(np.array(res.cpg_dual.d94).reshape((6, 3), order='F'))
    prob.constraints[95].save_dual_value(np.array(res.cpg_dual.d95).reshape(1))
    prob.constraints[96].save_dual_value(np.array(res.cpg_dual.d96).reshape((6, 3), order='F'))
    prob.constraints[97].save_dual_value(np.array(res.cpg_dual.d97).reshape(1))
    prob.constraints[98].save_dual_value(np.array(res.cpg_dual.d98).reshape((6, 3), order='F'))
    prob.constraints[99].save_dual_value(np.array(res.cpg_dual.d99).reshape(1))
    prob.constraints[100].save_dual_value(np.array(res.cpg_dual.d100).reshape((6, 3), order='F'))
    prob.constraints[101].save_dual_value(np.array(res.cpg_dual.d101).reshape(1))
    prob.constraints[102].save_dual_value(np.array(res.cpg_dual.d102).reshape((6, 3), order='F'))
    prob.constraints[103].save_dual_value(np.array(res.cpg_dual.d103).reshape(1))
    prob.constraints[104].save_dual_value(np.array(res.cpg_dual.d104).reshape((6, 3), order='F'))
    prob.constraints[105].save_dual_value(np.array(res.cpg_dual.d105).reshape(1))
    prob.constraints[106].save_dual_value(np.array(res.cpg_dual.d106).reshape((6, 3), order='F'))
    prob.constraints[107].save_dual_value(np.array(res.cpg_dual.d107).reshape(1))
    prob.constraints[108].save_dual_value(np.array(res.cpg_dual.d108).reshape((6, 3), order='F'))
    prob.constraints[109].save_dual_value(np.array(res.cpg_dual.d109).reshape(1))
    prob.constraints[110].save_dual_value(np.array(res.cpg_dual.d110).reshape((6, 3), order='F'))
    prob.constraints[111].save_dual_value(np.array(res.cpg_dual.d111).reshape(1))
    prob.constraints[112].save_dual_value(np.array(res.cpg_dual.d112).reshape((6, 3), order='F'))
    prob.constraints[113].save_dual_value(np.array(res.cpg_dual.d113).reshape(1))
    prob.constraints[114].save_dual_value(np.array(res.cpg_dual.d114).reshape((6, 3), order='F'))
    prob.constraints[115].save_dual_value(np.array(res.cpg_dual.d115).reshape(1))
    prob.constraints[116].save_dual_value(np.array(res.cpg_dual.d116).reshape((6, 3), order='F'))
    prob.constraints[117].save_dual_value(np.array(res.cpg_dual.d117).reshape(1))
    prob.constraints[118].save_dual_value(np.array(res.cpg_dual.d118).reshape((6, 3), order='F'))
    prob.constraints[119].save_dual_value(np.array(res.cpg_dual.d119).reshape(1))
    prob.constraints[120].save_dual_value(np.array(res.cpg_dual.d120).reshape((6, 3), order='F'))
    prob.constraints[121].save_dual_value(np.array(res.cpg_dual.d121).reshape(1))
    prob.constraints[122].save_dual_value(np.array(res.cpg_dual.d122).reshape((6, 3), order='F'))
    prob.constraints[123].save_dual_value(np.array(res.cpg_dual.d123).reshape(1))
    prob.constraints[124].save_dual_value(np.array(res.cpg_dual.d124).reshape((6, 3), order='F'))
    prob.constraints[125].save_dual_value(np.array(res.cpg_dual.d125).reshape(1))
    prob.constraints[126].save_dual_value(np.array(res.cpg_dual.d126).reshape((6, 3), order='F'))
    prob.constraints[127].save_dual_value(np.array(res.cpg_dual.d127).reshape(1))
    prob.constraints[128].save_dual_value(np.array(res.cpg_dual.d128).reshape((6, 3), order='F'))
    prob.constraints[129].save_dual_value(np.array(res.cpg_dual.d129).reshape(1))
    prob.constraints[130].save_dual_value(np.array(res.cpg_dual.d130).reshape((6, 3), order='F'))
    prob.constraints[131].save_dual_value(np.array(res.cpg_dual.d131).reshape(1))
    prob.constraints[132].save_dual_value(np.array(res.cpg_dual.d132).reshape((6, 3), order='F'))
    prob.constraints[133].save_dual_value(np.array(res.cpg_dual.d133).reshape(1))
    prob.constraints[134].save_dual_value(np.array(res.cpg_dual.d134).reshape((6, 3), order='F'))
    prob.constraints[135].save_dual_value(np.array(res.cpg_dual.d135).reshape(1))
    prob.constraints[136].save_dual_value(np.array(res.cpg_dual.d136).reshape((6, 3), order='F'))
    prob.constraints[137].save_dual_value(np.array(res.cpg_dual.d137).reshape(1))
    prob.constraints[138].save_dual_value(np.array(res.cpg_dual.d138).reshape((6, 3), order='F'))
    prob.constraints[139].save_dual_value(np.array(res.cpg_dual.d139).reshape(1))
    prob.constraints[140].save_dual_value(np.array(res.cpg_dual.d140).reshape((6, 3), order='F'))
    prob.constraints[141].save_dual_value(np.array(res.cpg_dual.d141).reshape(1))
    prob.constraints[142].save_dual_value(np.array(res.cpg_dual.d142).reshape((6, 3), order='F'))
    prob.constraints[143].save_dual_value(np.array(res.cpg_dual.d143).reshape(1))
    prob.constraints[144].save_dual_value(np.array(res.cpg_dual.d144).reshape((6, 3), order='F'))
    prob.constraints[145].save_dual_value(np.array(res.cpg_dual.d145).reshape(1))
    prob.constraints[146].save_dual_value(np.array(res.cpg_dual.d146).reshape((6, 3), order='F'))
    prob.constraints[147].save_dual_value(np.array(res.cpg_dual.d147).reshape(1))
    prob.constraints[148].save_dual_value(np.array(res.cpg_dual.d148).reshape((6, 3), order='F'))
    prob.constraints[149].save_dual_value(np.array(res.cpg_dual.d149).reshape(1))
    prob.constraints[150].save_dual_value(np.array(res.cpg_dual.d150).reshape((6, 3), order='F'))
    prob.constraints[151].save_dual_value(np.array(res.cpg_dual.d151).reshape(1))
    prob.constraints[152].save_dual_value(np.array(res.cpg_dual.d152).reshape((6, 3), order='F'))
    prob.constraints[153].save_dual_value(np.array(res.cpg_dual.d153).reshape(1))
    prob.constraints[154].save_dual_value(np.array(res.cpg_dual.d154).reshape((6, 3), order='F'))
    prob.constraints[155].save_dual_value(np.array(res.cpg_dual.d155).reshape(1))
    prob.constraints[156].save_dual_value(np.array(res.cpg_dual.d156).reshape((6, 3), order='F'))
    prob.constraints[157].save_dual_value(np.array(res.cpg_dual.d157).reshape(1))
    prob.constraints[158].save_dual_value(np.array(res.cpg_dual.d158).reshape((6, 3), order='F'))
    prob.constraints[159].save_dual_value(np.array(res.cpg_dual.d159).reshape(1))
    prob.constraints[160].save_dual_value(np.array(res.cpg_dual.d160).reshape((6, 3), order='F'))
    prob.constraints[161].save_dual_value(np.array(res.cpg_dual.d161).reshape(1))
    prob.constraints[162].save_dual_value(np.array(res.cpg_dual.d162).reshape((6, 3), order='F'))
    prob.constraints[163].save_dual_value(np.array(res.cpg_dual.d163).reshape(1))
    prob.constraints[164].save_dual_value(np.array(res.cpg_dual.d164).reshape((6, 3), order='F'))
    prob.constraints[165].save_dual_value(np.array(res.cpg_dual.d165).reshape(1))
    prob.constraints[166].save_dual_value(np.array(res.cpg_dual.d166).reshape((6, 3), order='F'))
    prob.constraints[167].save_dual_value(np.array(res.cpg_dual.d167).reshape(1))
    prob.constraints[168].save_dual_value(np.array(res.cpg_dual.d168).reshape((6, 3), order='F'))
    prob.constraints[169].save_dual_value(np.array(res.cpg_dual.d169).reshape(1))
    prob.constraints[170].save_dual_value(np.array(res.cpg_dual.d170).reshape((6, 3), order='F'))
    prob.constraints[171].save_dual_value(np.array(res.cpg_dual.d171).reshape(1))
    prob.constraints[172].save_dual_value(np.array(res.cpg_dual.d172).reshape((6, 3), order='F'))
    prob.constraints[173].save_dual_value(np.array(res.cpg_dual.d173).reshape(1))
    prob.constraints[174].save_dual_value(np.array(res.cpg_dual.d174).reshape((6, 3), order='F'))
    prob.constraints[175].save_dual_value(np.array(res.cpg_dual.d175).reshape(1))
    prob.constraints[176].save_dual_value(np.array(res.cpg_dual.d176).reshape((6, 3), order='F'))
    prob.constraints[177].save_dual_value(np.array(res.cpg_dual.d177).reshape(1))
    prob.constraints[178].save_dual_value(np.array(res.cpg_dual.d178).reshape((6, 3), order='F'))
    prob.constraints[179].save_dual_value(np.array(res.cpg_dual.d179).reshape(1))
    prob.constraints[180].save_dual_value(np.array(res.cpg_dual.d180).reshape((6, 3), order='F'))
    prob.constraints[181].save_dual_value(np.array(res.cpg_dual.d181).reshape(1))
    prob.constraints[182].save_dual_value(np.array(res.cpg_dual.d182).reshape((6, 3), order='F'))
    prob.constraints[183].save_dual_value(np.array(res.cpg_dual.d183).reshape(1))
    prob.constraints[184].save_dual_value(np.array(res.cpg_dual.d184).reshape((6, 3), order='F'))
    prob.constraints[185].save_dual_value(np.array(res.cpg_dual.d185).reshape(1))
    prob.constraints[186].save_dual_value(np.array(res.cpg_dual.d186).reshape((6, 3), order='F'))
    prob.constraints[187].save_dual_value(np.array(res.cpg_dual.d187).reshape(1))
    prob.constraints[188].save_dual_value(np.array(res.cpg_dual.d188).reshape((6, 3), order='F'))
    prob.constraints[189].save_dual_value(np.array(res.cpg_dual.d189).reshape(1))
    prob.constraints[190].save_dual_value(np.array(res.cpg_dual.d190).reshape((6, 3), order='F'))
    prob.constraints[191].save_dual_value(np.array(res.cpg_dual.d191).reshape(1))
    prob.constraints[192].save_dual_value(np.array(res.cpg_dual.d192).reshape((6, 3), order='F'))
    prob.constraints[193].save_dual_value(np.array(res.cpg_dual.d193).reshape(1))
    prob.constraints[194].save_dual_value(np.array(res.cpg_dual.d194).reshape((6, 3), order='F'))
    prob.constraints[195].save_dual_value(np.array(res.cpg_dual.d195).reshape(1))
    prob.constraints[196].save_dual_value(np.array(res.cpg_dual.d196).reshape((6, 3), order='F'))
    prob.constraints[197].save_dual_value(np.array(res.cpg_dual.d197).reshape(1))
    prob.constraints[198].save_dual_value(np.array(res.cpg_dual.d198).reshape((6, 3), order='F'))
    prob.constraints[199].save_dual_value(np.array(res.cpg_dual.d199).reshape(1))
    prob.constraints[200].save_dual_value(np.array(res.cpg_dual.d200).reshape((6, 3), order='F'))
    prob.constraints[201].save_dual_value(np.array(res.cpg_dual.d201).reshape(1))
    prob.constraints[202].save_dual_value(np.array(res.cpg_dual.d202).reshape((6, 3), order='F'))
    prob.constraints[203].save_dual_value(np.array(res.cpg_dual.d203).reshape(1))
    prob.constraints[204].save_dual_value(np.array(res.cpg_dual.d204).reshape((6, 3), order='F'))
    prob.constraints[205].save_dual_value(np.array(res.cpg_dual.d205).reshape(1))
    prob.constraints[206].save_dual_value(np.array(res.cpg_dual.d206).reshape((6, 3), order='F'))
    prob.constraints[207].save_dual_value(np.array(res.cpg_dual.d207).reshape(1))
    prob.constraints[208].save_dual_value(np.array(res.cpg_dual.d208).reshape((6, 3), order='F'))
    prob.constraints[209].save_dual_value(np.array(res.cpg_dual.d209).reshape(1))
    prob.constraints[210].save_dual_value(np.array(res.cpg_dual.d210).reshape((6, 3), order='F'))
    prob.constraints[211].save_dual_value(np.array(res.cpg_dual.d211).reshape(1))
    prob.constraints[212].save_dual_value(np.array(res.cpg_dual.d212).reshape((6, 3), order='F'))
    prob.constraints[213].save_dual_value(np.array(res.cpg_dual.d213).reshape(1))
    prob.constraints[214].save_dual_value(np.array(res.cpg_dual.d214).reshape((6, 3), order='F'))
    prob.constraints[215].save_dual_value(np.array(res.cpg_dual.d215).reshape(1))
    prob.constraints[216].save_dual_value(np.array(res.cpg_dual.d216).reshape((6, 3), order='F'))
    prob.constraints[217].save_dual_value(np.array(res.cpg_dual.d217).reshape(1))
    prob.constraints[218].save_dual_value(np.array(res.cpg_dual.d218).reshape((6, 3), order='F'))
    prob.constraints[219].save_dual_value(np.array(res.cpg_dual.d219).reshape(1))
    prob.constraints[220].save_dual_value(np.array(res.cpg_dual.d220).reshape((6, 3), order='F'))
    prob.constraints[221].save_dual_value(np.array(res.cpg_dual.d221).reshape(1))
    prob.constraints[222].save_dual_value(np.array(res.cpg_dual.d222).reshape((6, 3), order='F'))
    prob.constraints[223].save_dual_value(np.array(res.cpg_dual.d223).reshape(1))
    prob.constraints[224].save_dual_value(np.array(res.cpg_dual.d224).reshape((6, 3), order='F'))
    prob.constraints[225].save_dual_value(np.array(res.cpg_dual.d225).reshape(1))
    prob.constraints[226].save_dual_value(np.array(res.cpg_dual.d226).reshape((6, 3), order='F'))
    prob.constraints[227].save_dual_value(np.array(res.cpg_dual.d227).reshape(1))
    prob.constraints[228].save_dual_value(np.array(res.cpg_dual.d228).reshape((6, 3), order='F'))
    prob.constraints[229].save_dual_value(np.array(res.cpg_dual.d229).reshape(1))
    prob.constraints[230].save_dual_value(np.array(res.cpg_dual.d230).reshape((6, 3), order='F'))
    prob.constraints[231].save_dual_value(np.array(res.cpg_dual.d231).reshape(1))
    prob.constraints[232].save_dual_value(np.array(res.cpg_dual.d232).reshape((6, 3), order='F'))
    prob.constraints[233].save_dual_value(np.array(res.cpg_dual.d233).reshape(1))
    prob.constraints[234].save_dual_value(np.array(res.cpg_dual.d234).reshape((6, 3), order='F'))
    prob.constraints[235].save_dual_value(np.array(res.cpg_dual.d235).reshape(1))
    prob.constraints[236].save_dual_value(np.array(res.cpg_dual.d236).reshape((6, 3), order='F'))
    prob.constraints[237].save_dual_value(np.array(res.cpg_dual.d237).reshape(1))
    prob.constraints[238].save_dual_value(np.array(res.cpg_dual.d238).reshape((6, 3), order='F'))
    prob.constraints[239].save_dual_value(np.array(res.cpg_dual.d239).reshape(1))
    prob.constraints[240].save_dual_value(np.array(res.cpg_dual.d240).reshape((6, 3), order='F'))
    prob.constraints[241].save_dual_value(np.array(res.cpg_dual.d241).reshape(1))
    prob.constraints[242].save_dual_value(np.array(res.cpg_dual.d242).reshape((6, 3), order='F'))
    prob.constraints[243].save_dual_value(np.array(res.cpg_dual.d243).reshape(1))
    prob.constraints[244].save_dual_value(np.array(res.cpg_dual.d244).reshape((6, 3), order='F'))
    prob.constraints[245].save_dual_value(np.array(res.cpg_dual.d245).reshape(1))
    prob.constraints[246].save_dual_value(np.array(res.cpg_dual.d246).reshape(1))
    prob.constraints[247].save_dual_value(np.array(res.cpg_dual.d247))
    prob.constraints[248].save_dual_value(np.array(res.cpg_dual.d248).reshape(1))
    prob.constraints[249].save_dual_value(np.array(res.cpg_dual.d249).reshape(3))
    prob.constraints[250].save_dual_value(np.array(res.cpg_dual.d250).reshape(1))
    prob.constraints[251].save_dual_value(np.array(res.cpg_dual.d251).reshape(1))
    prob.constraints[252].save_dual_value(np.array(res.cpg_dual.d252).reshape(1))
    prob.constraints[253].save_dual_value(np.array(res.cpg_dual.d253))
    prob.constraints[254].save_dual_value(np.array(res.cpg_dual.d254).reshape(1))
    prob.constraints[255].save_dual_value(np.array(res.cpg_dual.d255).reshape(3))
    prob.constraints[256].save_dual_value(np.array(res.cpg_dual.d256).reshape(1))
    prob.constraints[257].save_dual_value(np.array(res.cpg_dual.d257).reshape(1))
    prob.constraints[258].save_dual_value(np.array(res.cpg_dual.d258).reshape(1))
    prob.constraints[259].save_dual_value(np.array(res.cpg_dual.d259))
    prob.constraints[260].save_dual_value(np.array(res.cpg_dual.d260).reshape(1))
    prob.constraints[261].save_dual_value(np.array(res.cpg_dual.d261).reshape(3))
    prob.constraints[262].save_dual_value(np.array(res.cpg_dual.d262).reshape(1))
    prob.constraints[263].save_dual_value(np.array(res.cpg_dual.d263).reshape(1))
    prob.constraints[264].save_dual_value(np.array(res.cpg_dual.d264).reshape(1))
    prob.constraints[265].save_dual_value(np.array(res.cpg_dual.d265))
    prob.constraints[266].save_dual_value(np.array(res.cpg_dual.d266).reshape(1))
    prob.constraints[267].save_dual_value(np.array(res.cpg_dual.d267).reshape(3))
    prob.constraints[268].save_dual_value(np.array(res.cpg_dual.d268).reshape(1))
    prob.constraints[269].save_dual_value(np.array(res.cpg_dual.d269).reshape(1))
    prob.constraints[270].save_dual_value(np.array(res.cpg_dual.d270).reshape(1))
    prob.constraints[271].save_dual_value(np.array(res.cpg_dual.d271))
    prob.constraints[272].save_dual_value(np.array(res.cpg_dual.d272).reshape(1))
    prob.constraints[273].save_dual_value(np.array(res.cpg_dual.d273).reshape(3))
    prob.constraints[274].save_dual_value(np.array(res.cpg_dual.d274).reshape(1))
    prob.constraints[275].save_dual_value(np.array(res.cpg_dual.d275).reshape(1))
    prob.constraints[276].save_dual_value(np.array(res.cpg_dual.d276).reshape(1))
    prob.constraints[277].save_dual_value(np.array(res.cpg_dual.d277))
    prob.constraints[278].save_dual_value(np.array(res.cpg_dual.d278).reshape(1))
    prob.constraints[279].save_dual_value(np.array(res.cpg_dual.d279).reshape(3))
    prob.constraints[280].save_dual_value(np.array(res.cpg_dual.d280).reshape(1))
    prob.constraints[281].save_dual_value(np.array(res.cpg_dual.d281).reshape(1))
    prob.constraints[282].save_dual_value(np.array(res.cpg_dual.d282).reshape(1))
    prob.constraints[283].save_dual_value(np.array(res.cpg_dual.d283))
    prob.constraints[284].save_dual_value(np.array(res.cpg_dual.d284).reshape(1))
    prob.constraints[285].save_dual_value(np.array(res.cpg_dual.d285).reshape(3))
    prob.constraints[286].save_dual_value(np.array(res.cpg_dual.d286).reshape(1))
    prob.constraints[287].save_dual_value(np.array(res.cpg_dual.d287).reshape(1))
    prob.constraints[288].save_dual_value(np.array(res.cpg_dual.d288).reshape(1))
    prob.constraints[289].save_dual_value(np.array(res.cpg_dual.d289))
    prob.constraints[290].save_dual_value(np.array(res.cpg_dual.d290).reshape(1))
    prob.constraints[291].save_dual_value(np.array(res.cpg_dual.d291).reshape(3))
    prob.constraints[292].save_dual_value(np.array(res.cpg_dual.d292).reshape(1))
    prob.constraints[293].save_dual_value(np.array(res.cpg_dual.d293).reshape(1))
    prob.constraints[294].save_dual_value(np.array(res.cpg_dual.d294).reshape(1))
    prob.constraints[295].save_dual_value(np.array(res.cpg_dual.d295))
    prob.constraints[296].save_dual_value(np.array(res.cpg_dual.d296).reshape(1))
    prob.constraints[297].save_dual_value(np.array(res.cpg_dual.d297).reshape(3))
    prob.constraints[298].save_dual_value(np.array(res.cpg_dual.d298).reshape(1))
    prob.constraints[299].save_dual_value(np.array(res.cpg_dual.d299).reshape(1))
    prob.constraints[300].save_dual_value(np.array(res.cpg_dual.d300).reshape(1))
    prob.constraints[301].save_dual_value(np.array(res.cpg_dual.d301))
    prob.constraints[302].save_dual_value(np.array(res.cpg_dual.d302).reshape(1))
    prob.constraints[303].save_dual_value(np.array(res.cpg_dual.d303).reshape(3))
    prob.constraints[304].save_dual_value(np.array(res.cpg_dual.d304).reshape(1))
    prob.constraints[305].save_dual_value(np.array(res.cpg_dual.d305).reshape(1))
    prob.constraints[306].save_dual_value(np.array(res.cpg_dual.d306).reshape(1))
    prob.constraints[307].save_dual_value(np.array(res.cpg_dual.d307))
    prob.constraints[308].save_dual_value(np.array(res.cpg_dual.d308).reshape(1))
    prob.constraints[309].save_dual_value(np.array(res.cpg_dual.d309).reshape(3))
    prob.constraints[310].save_dual_value(np.array(res.cpg_dual.d310).reshape(1))
    prob.constraints[311].save_dual_value(np.array(res.cpg_dual.d311).reshape(1))
    prob.constraints[312].save_dual_value(np.array(res.cpg_dual.d312).reshape(1))
    prob.constraints[313].save_dual_value(np.array(res.cpg_dual.d313))
    prob.constraints[314].save_dual_value(np.array(res.cpg_dual.d314).reshape(1))
    prob.constraints[315].save_dual_value(np.array(res.cpg_dual.d315).reshape(3))
    prob.constraints[316].save_dual_value(np.array(res.cpg_dual.d316).reshape(1))
    prob.constraints[317].save_dual_value(np.array(res.cpg_dual.d317).reshape(1))
    prob.constraints[318].save_dual_value(np.array(res.cpg_dual.d318).reshape(1))
    prob.constraints[319].save_dual_value(np.array(res.cpg_dual.d319))
    prob.constraints[320].save_dual_value(np.array(res.cpg_dual.d320).reshape(1))
    prob.constraints[321].save_dual_value(np.array(res.cpg_dual.d321).reshape(3))
    prob.constraints[322].save_dual_value(np.array(res.cpg_dual.d322).reshape(1))
    prob.constraints[323].save_dual_value(np.array(res.cpg_dual.d323).reshape(1))
    prob.constraints[324].save_dual_value(np.array(res.cpg_dual.d324).reshape(1))
    prob.constraints[325].save_dual_value(np.array(res.cpg_dual.d325))
    prob.constraints[326].save_dual_value(np.array(res.cpg_dual.d326).reshape(1))
    prob.constraints[327].save_dual_value(np.array(res.cpg_dual.d327).reshape(3))
    prob.constraints[328].save_dual_value(np.array(res.cpg_dual.d328).reshape(1))
    prob.constraints[329].save_dual_value(np.array(res.cpg_dual.d329).reshape(1))
    prob.constraints[330].save_dual_value(np.array(res.cpg_dual.d330).reshape(1))
    prob.constraints[331].save_dual_value(np.array(res.cpg_dual.d331))
    prob.constraints[332].save_dual_value(np.array(res.cpg_dual.d332).reshape(1))
    prob.constraints[333].save_dual_value(np.array(res.cpg_dual.d333).reshape(3))
    prob.constraints[334].save_dual_value(np.array(res.cpg_dual.d334).reshape(1))
    prob.constraints[335].save_dual_value(np.array(res.cpg_dual.d335).reshape(1))
    prob.constraints[336].save_dual_value(np.array(res.cpg_dual.d336).reshape(1))
    prob.constraints[337].save_dual_value(np.array(res.cpg_dual.d337))
    prob.constraints[338].save_dual_value(np.array(res.cpg_dual.d338).reshape(1))
    prob.constraints[339].save_dual_value(np.array(res.cpg_dual.d339).reshape(3))
    prob.constraints[340].save_dual_value(np.array(res.cpg_dual.d340).reshape(1))
    prob.constraints[341].save_dual_value(np.array(res.cpg_dual.d341).reshape(1))
    prob.constraints[342].save_dual_value(np.array(res.cpg_dual.d342).reshape(1))
    prob.constraints[343].save_dual_value(np.array(res.cpg_dual.d343))
    prob.constraints[344].save_dual_value(np.array(res.cpg_dual.d344).reshape(1))
    prob.constraints[345].save_dual_value(np.array(res.cpg_dual.d345).reshape(3))
    prob.constraints[346].save_dual_value(np.array(res.cpg_dual.d346).reshape(1))
    prob.constraints[347].save_dual_value(np.array(res.cpg_dual.d347).reshape(1))
    prob.constraints[348].save_dual_value(np.array(res.cpg_dual.d348).reshape(1))
    prob.constraints[349].save_dual_value(np.array(res.cpg_dual.d349))
    prob.constraints[350].save_dual_value(np.array(res.cpg_dual.d350).reshape(1))
    prob.constraints[351].save_dual_value(np.array(res.cpg_dual.d351).reshape(3))
    prob.constraints[352].save_dual_value(np.array(res.cpg_dual.d352).reshape(1))
    prob.constraints[353].save_dual_value(np.array(res.cpg_dual.d353).reshape(1))
    prob.constraints[354].save_dual_value(np.array(res.cpg_dual.d354).reshape(1))
    prob.constraints[355].save_dual_value(np.array(res.cpg_dual.d355))
    prob.constraints[356].save_dual_value(np.array(res.cpg_dual.d356).reshape(1))
    prob.constraints[357].save_dual_value(np.array(res.cpg_dual.d357).reshape(3))
    prob.constraints[358].save_dual_value(np.array(res.cpg_dual.d358).reshape(1))
    prob.constraints[359].save_dual_value(np.array(res.cpg_dual.d359).reshape(1))
    prob.constraints[360].save_dual_value(np.array(res.cpg_dual.d360).reshape(1))
    prob.constraints[361].save_dual_value(np.array(res.cpg_dual.d361))
    prob.constraints[362].save_dual_value(np.array(res.cpg_dual.d362).reshape(1))
    prob.constraints[363].save_dual_value(np.array(res.cpg_dual.d363).reshape(3))
    prob.constraints[364].save_dual_value(np.array(res.cpg_dual.d364).reshape(1))
    prob.constraints[365].save_dual_value(np.array(res.cpg_dual.d365).reshape(1))
    prob.constraints[366].save_dual_value(np.array(res.cpg_dual.d366).reshape(1))
    prob.constraints[367].save_dual_value(np.array(res.cpg_dual.d367))
    prob.constraints[368].save_dual_value(np.array(res.cpg_dual.d368).reshape(1))
    prob.constraints[369].save_dual_value(np.array(res.cpg_dual.d369).reshape(3))
    prob.constraints[370].save_dual_value(np.array(res.cpg_dual.d370).reshape(1))
    prob.constraints[371].save_dual_value(np.array(res.cpg_dual.d371).reshape(1))
    prob.constraints[372].save_dual_value(np.array(res.cpg_dual.d372).reshape(1))
    prob.constraints[373].save_dual_value(np.array(res.cpg_dual.d373))
    prob.constraints[374].save_dual_value(np.array(res.cpg_dual.d374).reshape(1))
    prob.constraints[375].save_dual_value(np.array(res.cpg_dual.d375).reshape(3))
    prob.constraints[376].save_dual_value(np.array(res.cpg_dual.d376).reshape(1))
    prob.constraints[377].save_dual_value(np.array(res.cpg_dual.d377).reshape(1))
    prob.constraints[378].save_dual_value(np.array(res.cpg_dual.d378).reshape(1))
    prob.constraints[379].save_dual_value(np.array(res.cpg_dual.d379))
    prob.constraints[380].save_dual_value(np.array(res.cpg_dual.d380).reshape(1))
    prob.constraints[381].save_dual_value(np.array(res.cpg_dual.d381).reshape(3))
    prob.constraints[382].save_dual_value(np.array(res.cpg_dual.d382).reshape(1))
    prob.constraints[383].save_dual_value(np.array(res.cpg_dual.d383).reshape(1))
    prob.constraints[384].save_dual_value(np.array(res.cpg_dual.d384).reshape(1))
    prob.constraints[385].save_dual_value(np.array(res.cpg_dual.d385))
    prob.constraints[386].save_dual_value(np.array(res.cpg_dual.d386).reshape(1))
    prob.constraints[387].save_dual_value(np.array(res.cpg_dual.d387).reshape(3))
    prob.constraints[388].save_dual_value(np.array(res.cpg_dual.d388).reshape(1))
    prob.constraints[389].save_dual_value(np.array(res.cpg_dual.d389).reshape(1))
    prob.constraints[390].save_dual_value(np.array(res.cpg_dual.d390).reshape(1))
    prob.constraints[391].save_dual_value(np.array(res.cpg_dual.d391))
    prob.constraints[392].save_dual_value(np.array(res.cpg_dual.d392).reshape(1))
    prob.constraints[393].save_dual_value(np.array(res.cpg_dual.d393).reshape(3))
    prob.constraints[394].save_dual_value(np.array(res.cpg_dual.d394).reshape(1))
    prob.constraints[395].save_dual_value(np.array(res.cpg_dual.d395).reshape(1))
    prob.constraints[396].save_dual_value(np.array(res.cpg_dual.d396).reshape(1))
    prob.constraints[397].save_dual_value(np.array(res.cpg_dual.d397))
    prob.constraints[398].save_dual_value(np.array(res.cpg_dual.d398).reshape(1))
    prob.constraints[399].save_dual_value(np.array(res.cpg_dual.d399).reshape(3))
    prob.constraints[400].save_dual_value(np.array(res.cpg_dual.d400).reshape(1))
    prob.constraints[401].save_dual_value(np.array(res.cpg_dual.d401).reshape(1))
    prob.constraints[402].save_dual_value(np.array(res.cpg_dual.d402).reshape(1))
    prob.constraints[403].save_dual_value(np.array(res.cpg_dual.d403))
    prob.constraints[404].save_dual_value(np.array(res.cpg_dual.d404).reshape(1))
    prob.constraints[405].save_dual_value(np.array(res.cpg_dual.d405).reshape(3))
    prob.constraints[406].save_dual_value(np.array(res.cpg_dual.d406).reshape(1))
    prob.constraints[407].save_dual_value(np.array(res.cpg_dual.d407).reshape(1))
    prob.constraints[408].save_dual_value(np.array(res.cpg_dual.d408).reshape(1))
    prob.constraints[409].save_dual_value(np.array(res.cpg_dual.d409))
    prob.constraints[410].save_dual_value(np.array(res.cpg_dual.d410).reshape(1))
    prob.constraints[411].save_dual_value(np.array(res.cpg_dual.d411).reshape(3))
    prob.constraints[412].save_dual_value(np.array(res.cpg_dual.d412).reshape(1))
    prob.constraints[413].save_dual_value(np.array(res.cpg_dual.d413).reshape(1))
    prob.constraints[414].save_dual_value(np.array(res.cpg_dual.d414).reshape(1))
    prob.constraints[415].save_dual_value(np.array(res.cpg_dual.d415))
    prob.constraints[416].save_dual_value(np.array(res.cpg_dual.d416).reshape(1))
    prob.constraints[417].save_dual_value(np.array(res.cpg_dual.d417).reshape(3))
    prob.constraints[418].save_dual_value(np.array(res.cpg_dual.d418).reshape(1))
    prob.constraints[419].save_dual_value(np.array(res.cpg_dual.d419).reshape(1))
    prob.constraints[420].save_dual_value(np.array(res.cpg_dual.d420).reshape(1))
    prob.constraints[421].save_dual_value(np.array(res.cpg_dual.d421))
    prob.constraints[422].save_dual_value(np.array(res.cpg_dual.d422).reshape(1))
    prob.constraints[423].save_dual_value(np.array(res.cpg_dual.d423).reshape(3))
    prob.constraints[424].save_dual_value(np.array(res.cpg_dual.d424).reshape(1))
    prob.constraints[425].save_dual_value(np.array(res.cpg_dual.d425).reshape(1))
    prob.constraints[426].save_dual_value(np.array(res.cpg_dual.d426).reshape(1))
    prob.constraints[427].save_dual_value(np.array(res.cpg_dual.d427))
    prob.constraints[428].save_dual_value(np.array(res.cpg_dual.d428).reshape(1))
    prob.constraints[429].save_dual_value(np.array(res.cpg_dual.d429).reshape(3))
    prob.constraints[430].save_dual_value(np.array(res.cpg_dual.d430).reshape(1))
    prob.constraints[431].save_dual_value(np.array(res.cpg_dual.d431).reshape(1))
    prob.constraints[432].save_dual_value(np.array(res.cpg_dual.d432).reshape(1))
    prob.constraints[433].save_dual_value(np.array(res.cpg_dual.d433))
    prob.constraints[434].save_dual_value(np.array(res.cpg_dual.d434).reshape(1))
    prob.constraints[435].save_dual_value(np.array(res.cpg_dual.d435).reshape(3))
    prob.constraints[436].save_dual_value(np.array(res.cpg_dual.d436).reshape(1))
    prob.constraints[437].save_dual_value(np.array(res.cpg_dual.d437).reshape(1))
    prob.constraints[438].save_dual_value(np.array(res.cpg_dual.d438).reshape(1))
    prob.constraints[439].save_dual_value(np.array(res.cpg_dual.d439))
    prob.constraints[440].save_dual_value(np.array(res.cpg_dual.d440).reshape(1))
    prob.constraints[441].save_dual_value(np.array(res.cpg_dual.d441).reshape(3))
    prob.constraints[442].save_dual_value(np.array(res.cpg_dual.d442).reshape(1))
    prob.constraints[443].save_dual_value(np.array(res.cpg_dual.d443).reshape(1))
    prob.constraints[444].save_dual_value(np.array(res.cpg_dual.d444).reshape(1))
    prob.constraints[445].save_dual_value(np.array(res.cpg_dual.d445))
    prob.constraints[446].save_dual_value(np.array(res.cpg_dual.d446).reshape(1))
    prob.constraints[447].save_dual_value(np.array(res.cpg_dual.d447).reshape(3))
    prob.constraints[448].save_dual_value(np.array(res.cpg_dual.d448).reshape(1))
    prob.constraints[449].save_dual_value(np.array(res.cpg_dual.d449).reshape(1))
    prob.constraints[450].save_dual_value(np.array(res.cpg_dual.d450).reshape(1))
    prob.constraints[451].save_dual_value(np.array(res.cpg_dual.d451))
    prob.constraints[452].save_dual_value(np.array(res.cpg_dual.d452).reshape(1))
    prob.constraints[453].save_dual_value(np.array(res.cpg_dual.d453).reshape(3))
    prob.constraints[454].save_dual_value(np.array(res.cpg_dual.d454).reshape(1))
    prob.constraints[455].save_dual_value(np.array(res.cpg_dual.d455).reshape(1))
    prob.constraints[456].save_dual_value(np.array(res.cpg_dual.d456).reshape(1))
    prob.constraints[457].save_dual_value(np.array(res.cpg_dual.d457))
    prob.constraints[458].save_dual_value(np.array(res.cpg_dual.d458).reshape(1))
    prob.constraints[459].save_dual_value(np.array(res.cpg_dual.d459).reshape(3))
    prob.constraints[460].save_dual_value(np.array(res.cpg_dual.d460).reshape(1))
    prob.constraints[461].save_dual_value(np.array(res.cpg_dual.d461).reshape(1))
    prob.constraints[462].save_dual_value(np.array(res.cpg_dual.d462).reshape(1))
    prob.constraints[463].save_dual_value(np.array(res.cpg_dual.d463))
    prob.constraints[464].save_dual_value(np.array(res.cpg_dual.d464).reshape(1))
    prob.constraints[465].save_dual_value(np.array(res.cpg_dual.d465).reshape(3))
    prob.constraints[466].save_dual_value(np.array(res.cpg_dual.d466).reshape(1))
    prob.constraints[467].save_dual_value(np.array(res.cpg_dual.d467).reshape(1))
    prob.constraints[468].save_dual_value(np.array(res.cpg_dual.d468).reshape(1))
    prob.constraints[469].save_dual_value(np.array(res.cpg_dual.d469))
    prob.constraints[470].save_dual_value(np.array(res.cpg_dual.d470).reshape(1))
    prob.constraints[471].save_dual_value(np.array(res.cpg_dual.d471).reshape(3))
    prob.constraints[472].save_dual_value(np.array(res.cpg_dual.d472).reshape(1))
    prob.constraints[473].save_dual_value(np.array(res.cpg_dual.d473).reshape(1))
    prob.constraints[474].save_dual_value(np.array(res.cpg_dual.d474).reshape(1))
    prob.constraints[475].save_dual_value(np.array(res.cpg_dual.d475))
    prob.constraints[476].save_dual_value(np.array(res.cpg_dual.d476).reshape(1))
    prob.constraints[477].save_dual_value(np.array(res.cpg_dual.d477).reshape(3))
    prob.constraints[478].save_dual_value(np.array(res.cpg_dual.d478).reshape(1))
    prob.constraints[479].save_dual_value(np.array(res.cpg_dual.d479).reshape(1))
    prob.constraints[480].save_dual_value(np.array(res.cpg_dual.d480).reshape(1))
    prob.constraints[481].save_dual_value(np.array(res.cpg_dual.d481))
    prob.constraints[482].save_dual_value(np.array(res.cpg_dual.d482).reshape(1))
    prob.constraints[483].save_dual_value(np.array(res.cpg_dual.d483).reshape(3))
    prob.constraints[484].save_dual_value(np.array(res.cpg_dual.d484).reshape(1))
    prob.constraints[485].save_dual_value(np.array(res.cpg_dual.d485).reshape(1))
    prob.constraints[486].save_dual_value(np.array(res.cpg_dual.d486).reshape(1))
    prob.constraints[487].save_dual_value(np.array(res.cpg_dual.d487))
    prob.constraints[488].save_dual_value(np.array(res.cpg_dual.d488).reshape(1))
    prob.constraints[489].save_dual_value(np.array(res.cpg_dual.d489).reshape(3))
    prob.constraints[490].save_dual_value(np.array(res.cpg_dual.d490).reshape(1))
    prob.constraints[491].save_dual_value(np.array(res.cpg_dual.d491).reshape(1))
    prob.constraints[492].save_dual_value(np.array(res.cpg_dual.d492).reshape(1))
    prob.constraints[493].save_dual_value(np.array(res.cpg_dual.d493))
    prob.constraints[494].save_dual_value(np.array(res.cpg_dual.d494).reshape(1))
    prob.constraints[495].save_dual_value(np.array(res.cpg_dual.d495).reshape(3))
    prob.constraints[496].save_dual_value(np.array(res.cpg_dual.d496).reshape(1))
    prob.constraints[497].save_dual_value(np.array(res.cpg_dual.d497).reshape(1))
    prob.constraints[498].save_dual_value(np.array(res.cpg_dual.d498).reshape(1))
    prob.constraints[499].save_dual_value(np.array(res.cpg_dual.d499))
    prob.constraints[500].save_dual_value(np.array(res.cpg_dual.d500).reshape(1))
    prob.constraints[501].save_dual_value(np.array(res.cpg_dual.d501).reshape(3))
    prob.constraints[502].save_dual_value(np.array(res.cpg_dual.d502).reshape(1))
    prob.constraints[503].save_dual_value(np.array(res.cpg_dual.d503).reshape(1))
    prob.constraints[504].save_dual_value(np.array(res.cpg_dual.d504).reshape(1))
    prob.constraints[505].save_dual_value(np.array(res.cpg_dual.d505))
    prob.constraints[506].save_dual_value(np.array(res.cpg_dual.d506).reshape(1))
    prob.constraints[507].save_dual_value(np.array(res.cpg_dual.d507).reshape(3))
    prob.constraints[508].save_dual_value(np.array(res.cpg_dual.d508).reshape(1))
    prob.constraints[509].save_dual_value(np.array(res.cpg_dual.d509).reshape(1))
    prob.constraints[510].save_dual_value(np.array(res.cpg_dual.d510).reshape(1))
    prob.constraints[511].save_dual_value(np.array(res.cpg_dual.d511))
    prob.constraints[512].save_dual_value(np.array(res.cpg_dual.d512).reshape(1))
    prob.constraints[513].save_dual_value(np.array(res.cpg_dual.d513).reshape(3))
    prob.constraints[514].save_dual_value(np.array(res.cpg_dual.d514).reshape(1))
    prob.constraints[515].save_dual_value(np.array(res.cpg_dual.d515).reshape(1))
    prob.constraints[516].save_dual_value(np.array(res.cpg_dual.d516).reshape(1))
    prob.constraints[517].save_dual_value(np.array(res.cpg_dual.d517))
    prob.constraints[518].save_dual_value(np.array(res.cpg_dual.d518).reshape(1))
    prob.constraints[519].save_dual_value(np.array(res.cpg_dual.d519).reshape(3))
    prob.constraints[520].save_dual_value(np.array(res.cpg_dual.d520).reshape(1))
    prob.constraints[521].save_dual_value(np.array(res.cpg_dual.d521).reshape(1))
    prob.constraints[522].save_dual_value(np.array(res.cpg_dual.d522).reshape(1))
    prob.constraints[523].save_dual_value(np.array(res.cpg_dual.d523))
    prob.constraints[524].save_dual_value(np.array(res.cpg_dual.d524).reshape(1))
    prob.constraints[525].save_dual_value(np.array(res.cpg_dual.d525).reshape(3))
    prob.constraints[526].save_dual_value(np.array(res.cpg_dual.d526).reshape(1))
    prob.constraints[527].save_dual_value(np.array(res.cpg_dual.d527).reshape(1))
    prob.constraints[528].save_dual_value(np.array(res.cpg_dual.d528).reshape(1))
    prob.constraints[529].save_dual_value(np.array(res.cpg_dual.d529))
    prob.constraints[530].save_dual_value(np.array(res.cpg_dual.d530).reshape(1))
    prob.constraints[531].save_dual_value(np.array(res.cpg_dual.d531).reshape(3))
    prob.constraints[532].save_dual_value(np.array(res.cpg_dual.d532).reshape(1))
    prob.constraints[533].save_dual_value(np.array(res.cpg_dual.d533).reshape(1))
    prob.constraints[534].save_dual_value(np.array(res.cpg_dual.d534).reshape(1))
    prob.constraints[535].save_dual_value(np.array(res.cpg_dual.d535))
    prob.constraints[536].save_dual_value(np.array(res.cpg_dual.d536).reshape(1))
    prob.constraints[537].save_dual_value(np.array(res.cpg_dual.d537).reshape(3))
    prob.constraints[538].save_dual_value(np.array(res.cpg_dual.d538).reshape(1))
    prob.constraints[539].save_dual_value(np.array(res.cpg_dual.d539).reshape(1))
    prob.constraints[540].save_dual_value(np.array(res.cpg_dual.d540).reshape(1))
    prob.constraints[541].save_dual_value(np.array(res.cpg_dual.d541))
    prob.constraints[542].save_dual_value(np.array(res.cpg_dual.d542).reshape(1))
    prob.constraints[543].save_dual_value(np.array(res.cpg_dual.d543).reshape(3))
    prob.constraints[544].save_dual_value(np.array(res.cpg_dual.d544).reshape(1))
    prob.constraints[545].save_dual_value(np.array(res.cpg_dual.d545).reshape(1))
    prob.constraints[546].save_dual_value(np.array(res.cpg_dual.d546).reshape(1))
    prob.constraints[547].save_dual_value(np.array(res.cpg_dual.d547))
    prob.constraints[548].save_dual_value(np.array(res.cpg_dual.d548).reshape(1))
    prob.constraints[549].save_dual_value(np.array(res.cpg_dual.d549).reshape(3))
    prob.constraints[550].save_dual_value(np.array(res.cpg_dual.d550).reshape(1))
    prob.constraints[551].save_dual_value(np.array(res.cpg_dual.d551).reshape(1))
    prob.constraints[552].save_dual_value(np.array(res.cpg_dual.d552).reshape(1))
    prob.constraints[553].save_dual_value(np.array(res.cpg_dual.d553))
    prob.constraints[554].save_dual_value(np.array(res.cpg_dual.d554).reshape(1))
    prob.constraints[555].save_dual_value(np.array(res.cpg_dual.d555).reshape(3))
    prob.constraints[556].save_dual_value(np.array(res.cpg_dual.d556).reshape(1))
    prob.constraints[557].save_dual_value(np.array(res.cpg_dual.d557).reshape(1))
    prob.constraints[558].save_dual_value(np.array(res.cpg_dual.d558).reshape(1))
    prob.constraints[559].save_dual_value(np.array(res.cpg_dual.d559))
    prob.constraints[560].save_dual_value(np.array(res.cpg_dual.d560).reshape(1))
    prob.constraints[561].save_dual_value(np.array(res.cpg_dual.d561).reshape(3))
    prob.constraints[562].save_dual_value(np.array(res.cpg_dual.d562).reshape(1))
    prob.constraints[563].save_dual_value(np.array(res.cpg_dual.d563).reshape(1))
    prob.constraints[564].save_dual_value(np.array(res.cpg_dual.d564).reshape(1))
    prob.constraints[565].save_dual_value(np.array(res.cpg_dual.d565))
    prob.constraints[566].save_dual_value(np.array(res.cpg_dual.d566).reshape(1))
    prob.constraints[567].save_dual_value(np.array(res.cpg_dual.d567).reshape(3))
    prob.constraints[568].save_dual_value(np.array(res.cpg_dual.d568).reshape(1))
    prob.constraints[569].save_dual_value(np.array(res.cpg_dual.d569).reshape(1))
    prob.constraints[570].save_dual_value(np.array(res.cpg_dual.d570).reshape(1))
    prob.constraints[571].save_dual_value(np.array(res.cpg_dual.d571))
    prob.constraints[572].save_dual_value(np.array(res.cpg_dual.d572).reshape(1))
    prob.constraints[573].save_dual_value(np.array(res.cpg_dual.d573).reshape(3))
    prob.constraints[574].save_dual_value(np.array(res.cpg_dual.d574).reshape(1))
    prob.constraints[575].save_dual_value(np.array(res.cpg_dual.d575).reshape(1))
    prob.constraints[576].save_dual_value(np.array(res.cpg_dual.d576).reshape(1))
    prob.constraints[577].save_dual_value(np.array(res.cpg_dual.d577))
    prob.constraints[578].save_dual_value(np.array(res.cpg_dual.d578).reshape(1))
    prob.constraints[579].save_dual_value(np.array(res.cpg_dual.d579).reshape(3))
    prob.constraints[580].save_dual_value(np.array(res.cpg_dual.d580).reshape(1))
    prob.constraints[581].save_dual_value(np.array(res.cpg_dual.d581).reshape(1))
    prob.constraints[582].save_dual_value(np.array(res.cpg_dual.d582).reshape(1))
    prob.constraints[583].save_dual_value(np.array(res.cpg_dual.d583))
    prob.constraints[584].save_dual_value(np.array(res.cpg_dual.d584).reshape(1))
    prob.constraints[585].save_dual_value(np.array(res.cpg_dual.d585).reshape(3))
    prob.constraints[586].save_dual_value(np.array(res.cpg_dual.d586).reshape(1))
    prob.constraints[587].save_dual_value(np.array(res.cpg_dual.d587).reshape(1))
    prob.constraints[588].save_dual_value(np.array(res.cpg_dual.d588).reshape(1))
    prob.constraints[589].save_dual_value(np.array(res.cpg_dual.d589))
    prob.constraints[590].save_dual_value(np.array(res.cpg_dual.d590).reshape(1))
    prob.constraints[591].save_dual_value(np.array(res.cpg_dual.d591).reshape(3))
    prob.constraints[592].save_dual_value(np.array(res.cpg_dual.d592).reshape(1))
    prob.constraints[593].save_dual_value(np.array(res.cpg_dual.d593).reshape(1))
    prob.constraints[594].save_dual_value(np.array(res.cpg_dual.d594).reshape(1))
    prob.constraints[595].save_dual_value(np.array(res.cpg_dual.d595))
    prob.constraints[596].save_dual_value(np.array(res.cpg_dual.d596).reshape(1))
    prob.constraints[597].save_dual_value(np.array(res.cpg_dual.d597).reshape(3))
    prob.constraints[598].save_dual_value(np.array(res.cpg_dual.d598).reshape(1))
    prob.constraints[599].save_dual_value(np.array(res.cpg_dual.d599).reshape(1))
    prob.constraints[600].save_dual_value(np.array(res.cpg_dual.d600).reshape(1))
    prob.constraints[601].save_dual_value(np.array(res.cpg_dual.d601))
    prob.constraints[602].save_dual_value(np.array(res.cpg_dual.d602).reshape(1))
    prob.constraints[603].save_dual_value(np.array(res.cpg_dual.d603).reshape(3))
    prob.constraints[604].save_dual_value(np.array(res.cpg_dual.d604).reshape(1))
    prob.constraints[605].save_dual_value(np.array(res.cpg_dual.d605).reshape(1))
    prob.constraints[606].save_dual_value(np.array(res.cpg_dual.d606).reshape(1))
    prob.constraints[607].save_dual_value(np.array(res.cpg_dual.d607))
    prob.constraints[608].save_dual_value(np.array(res.cpg_dual.d608).reshape(1))
    prob.constraints[609].save_dual_value(np.array(res.cpg_dual.d609).reshape(3))
    prob.constraints[610].save_dual_value(np.array(res.cpg_dual.d610).reshape(1))
    prob.constraints[611].save_dual_value(np.array(res.cpg_dual.d611).reshape(1))
    prob.constraints[612].save_dual_value(np.array(res.cpg_dual.d612).reshape(1))
    prob.constraints[613].save_dual_value(np.array(res.cpg_dual.d613))
    prob.constraints[614].save_dual_value(np.array(res.cpg_dual.d614).reshape(1))
    prob.constraints[615].save_dual_value(np.array(res.cpg_dual.d615).reshape(3))
    prob.constraints[616].save_dual_value(np.array(res.cpg_dual.d616).reshape(1))
    prob.constraints[617].save_dual_value(np.array(res.cpg_dual.d617).reshape(1))
    prob.constraints[618].save_dual_value(np.array(res.cpg_dual.d618).reshape(1))
    prob.constraints[619].save_dual_value(np.array(res.cpg_dual.d619))
    prob.constraints[620].save_dual_value(np.array(res.cpg_dual.d620).reshape(1))
    prob.constraints[621].save_dual_value(np.array(res.cpg_dual.d621).reshape(3))
    prob.constraints[622].save_dual_value(np.array(res.cpg_dual.d622).reshape(1))
    prob.constraints[623].save_dual_value(np.array(res.cpg_dual.d623).reshape(1))
    prob.constraints[624].save_dual_value(np.array(res.cpg_dual.d624).reshape(1))
    prob.constraints[625].save_dual_value(np.array(res.cpg_dual.d625))
    prob.constraints[626].save_dual_value(np.array(res.cpg_dual.d626).reshape(1))
    prob.constraints[627].save_dual_value(np.array(res.cpg_dual.d627).reshape(3))
    prob.constraints[628].save_dual_value(np.array(res.cpg_dual.d628).reshape(1))
    prob.constraints[629].save_dual_value(np.array(res.cpg_dual.d629).reshape(1))
    prob.constraints[630].save_dual_value(np.array(res.cpg_dual.d630).reshape(1))
    prob.constraints[631].save_dual_value(np.array(res.cpg_dual.d631))
    prob.constraints[632].save_dual_value(np.array(res.cpg_dual.d632).reshape(1))
    prob.constraints[633].save_dual_value(np.array(res.cpg_dual.d633).reshape(3))
    prob.constraints[634].save_dual_value(np.array(res.cpg_dual.d634).reshape(1))
    prob.constraints[635].save_dual_value(np.array(res.cpg_dual.d635).reshape(1))
    prob.constraints[636].save_dual_value(np.array(res.cpg_dual.d636).reshape(1))
    prob.constraints[637].save_dual_value(np.array(res.cpg_dual.d637))
    prob.constraints[638].save_dual_value(np.array(res.cpg_dual.d638).reshape(1))
    prob.constraints[639].save_dual_value(np.array(res.cpg_dual.d639).reshape(3))
    prob.constraints[640].save_dual_value(np.array(res.cpg_dual.d640).reshape(1))
    prob.constraints[641].save_dual_value(np.array(res.cpg_dual.d641).reshape(1))
    prob.constraints[642].save_dual_value(np.array(res.cpg_dual.d642).reshape(1))
    prob.constraints[643].save_dual_value(np.array(res.cpg_dual.d643))
    prob.constraints[644].save_dual_value(np.array(res.cpg_dual.d644).reshape(1))
    prob.constraints[645].save_dual_value(np.array(res.cpg_dual.d645).reshape(3))
    prob.constraints[646].save_dual_value(np.array(res.cpg_dual.d646).reshape(1))
    prob.constraints[647].save_dual_value(np.array(res.cpg_dual.d647).reshape(1))
    prob.constraints[648].save_dual_value(np.array(res.cpg_dual.d648).reshape(1))
    prob.constraints[649].save_dual_value(np.array(res.cpg_dual.d649))
    prob.constraints[650].save_dual_value(np.array(res.cpg_dual.d650).reshape(1))
    prob.constraints[651].save_dual_value(np.array(res.cpg_dual.d651).reshape(3))
    prob.constraints[652].save_dual_value(np.array(res.cpg_dual.d652).reshape(1))
    prob.constraints[653].save_dual_value(np.array(res.cpg_dual.d653).reshape(1))
    prob.constraints[654].save_dual_value(np.array(res.cpg_dual.d654).reshape(1))
    prob.constraints[655].save_dual_value(np.array(res.cpg_dual.d655))
    prob.constraints[656].save_dual_value(np.array(res.cpg_dual.d656).reshape(1))
    prob.constraints[657].save_dual_value(np.array(res.cpg_dual.d657).reshape(3))
    prob.constraints[658].save_dual_value(np.array(res.cpg_dual.d658).reshape(1))
    prob.constraints[659].save_dual_value(np.array(res.cpg_dual.d659).reshape(1))
    prob.constraints[660].save_dual_value(np.array(res.cpg_dual.d660).reshape(1))
    prob.constraints[661].save_dual_value(np.array(res.cpg_dual.d661))
    prob.constraints[662].save_dual_value(np.array(res.cpg_dual.d662).reshape(1))
    prob.constraints[663].save_dual_value(np.array(res.cpg_dual.d663).reshape(3))
    prob.constraints[664].save_dual_value(np.array(res.cpg_dual.d664).reshape(1))
    prob.constraints[665].save_dual_value(np.array(res.cpg_dual.d665).reshape(1))
    prob.constraints[666].save_dual_value(np.array(res.cpg_dual.d666).reshape(1))
    prob.constraints[667].save_dual_value(np.array(res.cpg_dual.d667))
    prob.constraints[668].save_dual_value(np.array(res.cpg_dual.d668).reshape(1))
    prob.constraints[669].save_dual_value(np.array(res.cpg_dual.d669).reshape(3))
    prob.constraints[670].save_dual_value(np.array(res.cpg_dual.d670).reshape(1))
    prob.constraints[671].save_dual_value(np.array(res.cpg_dual.d671).reshape(1))
    prob.constraints[672].save_dual_value(np.array(res.cpg_dual.d672).reshape(1))
    prob.constraints[673].save_dual_value(np.array(res.cpg_dual.d673))
    prob.constraints[674].save_dual_value(np.array(res.cpg_dual.d674).reshape(1))
    prob.constraints[675].save_dual_value(np.array(res.cpg_dual.d675).reshape(3))
    prob.constraints[676].save_dual_value(np.array(res.cpg_dual.d676).reshape(1))
    prob.constraints[677].save_dual_value(np.array(res.cpg_dual.d677).reshape(1))
    prob.constraints[678].save_dual_value(np.array(res.cpg_dual.d678).reshape(1))
    prob.constraints[679].save_dual_value(np.array(res.cpg_dual.d679))
    prob.constraints[680].save_dual_value(np.array(res.cpg_dual.d680).reshape(1))
    prob.constraints[681].save_dual_value(np.array(res.cpg_dual.d681).reshape(3))
    prob.constraints[682].save_dual_value(np.array(res.cpg_dual.d682).reshape(1))
    prob.constraints[683].save_dual_value(np.array(res.cpg_dual.d683).reshape(1))
    prob.constraints[684].save_dual_value(np.array(res.cpg_dual.d684).reshape(1))
    prob.constraints[685].save_dual_value(np.array(res.cpg_dual.d685))
    prob.constraints[686].save_dual_value(np.array(res.cpg_dual.d686).reshape(1))
    prob.constraints[687].save_dual_value(np.array(res.cpg_dual.d687).reshape(3))
    prob.constraints[688].save_dual_value(np.array(res.cpg_dual.d688).reshape(1))
    prob.constraints[689].save_dual_value(np.array(res.cpg_dual.d689).reshape(1))
    prob.constraints[690].save_dual_value(np.array(res.cpg_dual.d690).reshape(1))
    prob.constraints[691].save_dual_value(np.array(res.cpg_dual.d691))
    prob.constraints[692].save_dual_value(np.array(res.cpg_dual.d692).reshape(1))
    prob.constraints[693].save_dual_value(np.array(res.cpg_dual.d693).reshape(3))
    prob.constraints[694].save_dual_value(np.array(res.cpg_dual.d694).reshape(1))
    prob.constraints[695].save_dual_value(np.array(res.cpg_dual.d695).reshape(1))
    prob.constraints[696].save_dual_value(np.array(res.cpg_dual.d696).reshape(1))
    prob.constraints[697].save_dual_value(np.array(res.cpg_dual.d697))
    prob.constraints[698].save_dual_value(np.array(res.cpg_dual.d698).reshape(1))
    prob.constraints[699].save_dual_value(np.array(res.cpg_dual.d699).reshape(3))
    prob.constraints[700].save_dual_value(np.array(res.cpg_dual.d700).reshape(1))
    prob.constraints[701].save_dual_value(np.array(res.cpg_dual.d701).reshape(1))
    prob.constraints[702].save_dual_value(np.array(res.cpg_dual.d702).reshape(1))
    prob.constraints[703].save_dual_value(np.array(res.cpg_dual.d703))
    prob.constraints[704].save_dual_value(np.array(res.cpg_dual.d704).reshape(1))
    prob.constraints[705].save_dual_value(np.array(res.cpg_dual.d705).reshape(3))
    prob.constraints[706].save_dual_value(np.array(res.cpg_dual.d706).reshape(1))
    prob.constraints[707].save_dual_value(np.array(res.cpg_dual.d707).reshape(1))
    prob.constraints[708].save_dual_value(np.array(res.cpg_dual.d708).reshape(1))
    prob.constraints[709].save_dual_value(np.array(res.cpg_dual.d709))
    prob.constraints[710].save_dual_value(np.array(res.cpg_dual.d710).reshape(1))
    prob.constraints[711].save_dual_value(np.array(res.cpg_dual.d711).reshape(3))
    prob.constraints[712].save_dual_value(np.array(res.cpg_dual.d712).reshape(1))
    prob.constraints[713].save_dual_value(np.array(res.cpg_dual.d713).reshape(1))
    prob.constraints[714].save_dual_value(np.array(res.cpg_dual.d714).reshape(1))
    prob.constraints[715].save_dual_value(np.array(res.cpg_dual.d715))
    prob.constraints[716].save_dual_value(np.array(res.cpg_dual.d716).reshape(1))
    prob.constraints[717].save_dual_value(np.array(res.cpg_dual.d717).reshape(3))
    prob.constraints[718].save_dual_value(np.array(res.cpg_dual.d718).reshape(1))
    prob.constraints[719].save_dual_value(np.array(res.cpg_dual.d719).reshape(1))
    prob.constraints[720].save_dual_value(np.array(res.cpg_dual.d720).reshape(1))
    prob.constraints[721].save_dual_value(np.array(res.cpg_dual.d721))
    prob.constraints[722].save_dual_value(np.array(res.cpg_dual.d722).reshape(1))
    prob.constraints[723].save_dual_value(np.array(res.cpg_dual.d723).reshape(3))
    prob.constraints[724].save_dual_value(np.array(res.cpg_dual.d724).reshape(1))
    prob.constraints[725].save_dual_value(np.array(res.cpg_dual.d725).reshape(1))
    prob.constraints[726].save_dual_value(np.array(res.cpg_dual.d726).reshape(1))
    prob.constraints[727].save_dual_value(np.array(res.cpg_dual.d727))
    prob.constraints[728].save_dual_value(np.array(res.cpg_dual.d728).reshape(1))
    prob.constraints[729].save_dual_value(np.array(res.cpg_dual.d729).reshape(3))
    prob.constraints[730].save_dual_value(np.array(res.cpg_dual.d730).reshape(1))
    prob.constraints[731].save_dual_value(np.array(res.cpg_dual.d731).reshape(1))
    prob.constraints[732].save_dual_value(np.array(res.cpg_dual.d732).reshape(1))
    prob.constraints[733].save_dual_value(np.array(res.cpg_dual.d733))
    prob.constraints[734].save_dual_value(np.array(res.cpg_dual.d734).reshape(1))
    prob.constraints[735].save_dual_value(np.array(res.cpg_dual.d735).reshape(3))
    prob.constraints[736].save_dual_value(np.array(res.cpg_dual.d736).reshape(1))
    prob.constraints[737].save_dual_value(np.array(res.cpg_dual.d737).reshape(1))
    prob.constraints[738].save_dual_value(np.array(res.cpg_dual.d738).reshape(1))
    prob.constraints[739].save_dual_value(np.array(res.cpg_dual.d739))
    prob.constraints[740].save_dual_value(np.array(res.cpg_dual.d740).reshape(1))
    prob.constraints[741].save_dual_value(np.array(res.cpg_dual.d741).reshape(3))
    prob.constraints[742].save_dual_value(np.array(res.cpg_dual.d742).reshape(1))
    prob.constraints[743].save_dual_value(np.array(res.cpg_dual.d743).reshape(1))
    prob.constraints[744].save_dual_value(np.array(res.cpg_dual.d744).reshape(1))
    prob.constraints[745].save_dual_value(np.array(res.cpg_dual.d745))
    prob.constraints[746].save_dual_value(np.array(res.cpg_dual.d746).reshape(1))
    prob.constraints[747].save_dual_value(np.array(res.cpg_dual.d747).reshape(3))
    prob.constraints[748].save_dual_value(np.array(res.cpg_dual.d748).reshape(1))
    prob.constraints[749].save_dual_value(np.array(res.cpg_dual.d749).reshape(1))
    prob.constraints[750].save_dual_value(np.array(res.cpg_dual.d750).reshape(1))
    prob.constraints[751].save_dual_value(np.array(res.cpg_dual.d751))
    prob.constraints[752].save_dual_value(np.array(res.cpg_dual.d752).reshape(1))
    prob.constraints[753].save_dual_value(np.array(res.cpg_dual.d753).reshape(3))
    prob.constraints[754].save_dual_value(np.array(res.cpg_dual.d754).reshape(1))
    prob.constraints[755].save_dual_value(np.array(res.cpg_dual.d755).reshape(1))
    prob.constraints[756].save_dual_value(np.array(res.cpg_dual.d756).reshape(1))
    prob.constraints[757].save_dual_value(np.array(res.cpg_dual.d757))
    prob.constraints[758].save_dual_value(np.array(res.cpg_dual.d758).reshape(1))
    prob.constraints[759].save_dual_value(np.array(res.cpg_dual.d759).reshape(3))
    prob.constraints[760].save_dual_value(np.array(res.cpg_dual.d760).reshape(1))
    prob.constraints[761].save_dual_value(np.array(res.cpg_dual.d761).reshape(1))
    prob.constraints[762].save_dual_value(np.array(res.cpg_dual.d762).reshape(1))
    prob.constraints[763].save_dual_value(np.array(res.cpg_dual.d763))
    prob.constraints[764].save_dual_value(np.array(res.cpg_dual.d764).reshape(1))
    prob.constraints[765].save_dual_value(np.array(res.cpg_dual.d765).reshape(3))
    prob.constraints[766].save_dual_value(np.array(res.cpg_dual.d766).reshape(1))
    prob.constraints[767].save_dual_value(np.array(res.cpg_dual.d767).reshape(1))
    prob.constraints[768].save_dual_value(np.array(res.cpg_dual.d768).reshape(1))
    prob.constraints[769].save_dual_value(np.array(res.cpg_dual.d769))
    prob.constraints[770].save_dual_value(np.array(res.cpg_dual.d770).reshape(1))
    prob.constraints[771].save_dual_value(np.array(res.cpg_dual.d771).reshape(3))
    prob.constraints[772].save_dual_value(np.array(res.cpg_dual.d772).reshape(1))
    prob.constraints[773].save_dual_value(np.array(res.cpg_dual.d773).reshape(1))
    prob.constraints[774].save_dual_value(np.array(res.cpg_dual.d774).reshape(1))
    prob.constraints[775].save_dual_value(np.array(res.cpg_dual.d775))
    prob.constraints[776].save_dual_value(np.array(res.cpg_dual.d776).reshape(1))
    prob.constraints[777].save_dual_value(np.array(res.cpg_dual.d777).reshape(3))
    prob.constraints[778].save_dual_value(np.array(res.cpg_dual.d778).reshape(1))
    prob.constraints[779].save_dual_value(np.array(res.cpg_dual.d779).reshape(1))
    prob.constraints[780].save_dual_value(np.array(res.cpg_dual.d780).reshape(1))
    prob.constraints[781].save_dual_value(np.array(res.cpg_dual.d781))
    prob.constraints[782].save_dual_value(np.array(res.cpg_dual.d782).reshape(1))
    prob.constraints[783].save_dual_value(np.array(res.cpg_dual.d783).reshape(3))
    prob.constraints[784].save_dual_value(np.array(res.cpg_dual.d784).reshape(1))
    prob.constraints[785].save_dual_value(np.array(res.cpg_dual.d785).reshape(1))
    prob.constraints[786].save_dual_value(np.array(res.cpg_dual.d786).reshape(1))
    prob.constraints[787].save_dual_value(np.array(res.cpg_dual.d787))
    prob.constraints[788].save_dual_value(np.array(res.cpg_dual.d788).reshape(1))
    prob.constraints[789].save_dual_value(np.array(res.cpg_dual.d789).reshape(3))
    prob.constraints[790].save_dual_value(np.array(res.cpg_dual.d790).reshape(1))
    prob.constraints[791].save_dual_value(np.array(res.cpg_dual.d791).reshape(1))
    prob.constraints[792].save_dual_value(np.array(res.cpg_dual.d792).reshape(1))
    prob.constraints[793].save_dual_value(np.array(res.cpg_dual.d793))
    prob.constraints[794].save_dual_value(np.array(res.cpg_dual.d794).reshape(1))
    prob.constraints[795].save_dual_value(np.array(res.cpg_dual.d795).reshape(3))
    prob.constraints[796].save_dual_value(np.array(res.cpg_dual.d796).reshape(1))
    prob.constraints[797].save_dual_value(np.array(res.cpg_dual.d797).reshape(1))
    prob.constraints[798].save_dual_value(np.array(res.cpg_dual.d798).reshape(1))
    prob.constraints[799].save_dual_value(np.array(res.cpg_dual.d799))
    prob.constraints[800].save_dual_value(np.array(res.cpg_dual.d800).reshape(1))
    prob.constraints[801].save_dual_value(np.array(res.cpg_dual.d801).reshape(3))
    prob.constraints[802].save_dual_value(np.array(res.cpg_dual.d802).reshape(1))
    prob.constraints[803].save_dual_value(np.array(res.cpg_dual.d803).reshape(1))
    prob.constraints[804].save_dual_value(np.array(res.cpg_dual.d804).reshape(1))
    prob.constraints[805].save_dual_value(np.array(res.cpg_dual.d805))
    prob.constraints[806].save_dual_value(np.array(res.cpg_dual.d806).reshape(1))
    prob.constraints[807].save_dual_value(np.array(res.cpg_dual.d807).reshape(3))
    prob.constraints[808].save_dual_value(np.array(res.cpg_dual.d808).reshape(1))
    prob.constraints[809].save_dual_value(np.array(res.cpg_dual.d809).reshape(1))
    prob.constraints[810].save_dual_value(np.array(res.cpg_dual.d810).reshape(1))
    prob.constraints[811].save_dual_value(np.array(res.cpg_dual.d811))
    prob.constraints[812].save_dual_value(np.array(res.cpg_dual.d812).reshape(1))
    prob.constraints[813].save_dual_value(np.array(res.cpg_dual.d813).reshape(3))
    prob.constraints[814].save_dual_value(np.array(res.cpg_dual.d814).reshape(1))
    prob.constraints[815].save_dual_value(np.array(res.cpg_dual.d815).reshape(1))
    prob.constraints[816].save_dual_value(np.array(res.cpg_dual.d816).reshape(1))
    prob.constraints[817].save_dual_value(np.array(res.cpg_dual.d817))
    prob.constraints[818].save_dual_value(np.array(res.cpg_dual.d818).reshape(1))
    prob.constraints[819].save_dual_value(np.array(res.cpg_dual.d819).reshape(3))
    prob.constraints[820].save_dual_value(np.array(res.cpg_dual.d820).reshape(1))
    prob.constraints[821].save_dual_value(np.array(res.cpg_dual.d821).reshape(1))
    prob.constraints[822].save_dual_value(np.array(res.cpg_dual.d822).reshape(1))
    prob.constraints[823].save_dual_value(np.array(res.cpg_dual.d823))
    prob.constraints[824].save_dual_value(np.array(res.cpg_dual.d824).reshape(1))
    prob.constraints[825].save_dual_value(np.array(res.cpg_dual.d825).reshape(3))
    prob.constraints[826].save_dual_value(np.array(res.cpg_dual.d826).reshape(1))
    prob.constraints[827].save_dual_value(np.array(res.cpg_dual.d827).reshape(1))
    prob.constraints[828].save_dual_value(np.array(res.cpg_dual.d828).reshape(1))
    prob.constraints[829].save_dual_value(np.array(res.cpg_dual.d829))
    prob.constraints[830].save_dual_value(np.array(res.cpg_dual.d830).reshape(1))
    prob.constraints[831].save_dual_value(np.array(res.cpg_dual.d831).reshape(3))
    prob.constraints[832].save_dual_value(np.array(res.cpg_dual.d832).reshape(1))
    prob.constraints[833].save_dual_value(np.array(res.cpg_dual.d833).reshape(1))
    prob.constraints[834].save_dual_value(np.array(res.cpg_dual.d834).reshape(1))
    prob.constraints[835].save_dual_value(np.array(res.cpg_dual.d835))
    prob.constraints[836].save_dual_value(np.array(res.cpg_dual.d836).reshape(1))
    prob.constraints[837].save_dual_value(np.array(res.cpg_dual.d837).reshape(3))
    prob.constraints[838].save_dual_value(np.array(res.cpg_dual.d838).reshape(1))
    prob.constraints[839].save_dual_value(np.array(res.cpg_dual.d839).reshape(1))
    prob.constraints[840].save_dual_value(np.array(res.cpg_dual.d840).reshape(1))
    prob.constraints[841].save_dual_value(np.array(res.cpg_dual.d841))
    prob.constraints[842].save_dual_value(np.array(res.cpg_dual.d842).reshape(1))
    prob.constraints[843].save_dual_value(np.array(res.cpg_dual.d843).reshape(3))
    prob.constraints[844].save_dual_value(np.array(res.cpg_dual.d844).reshape(1))
    prob.constraints[845].save_dual_value(np.array(res.cpg_dual.d845).reshape(1))
    prob.constraints[846].save_dual_value(np.array(res.cpg_dual.d846).reshape(1))
    prob.constraints[847].save_dual_value(np.array(res.cpg_dual.d847))
    prob.constraints[848].save_dual_value(np.array(res.cpg_dual.d848).reshape(1))
    prob.constraints[849].save_dual_value(np.array(res.cpg_dual.d849).reshape(3))
    prob.constraints[850].save_dual_value(np.array(res.cpg_dual.d850).reshape(1))
    prob.constraints[851].save_dual_value(np.array(res.cpg_dual.d851).reshape(1))
    prob.constraints[852].save_dual_value(np.array(res.cpg_dual.d852).reshape(1))
    prob.constraints[853].save_dual_value(np.array(res.cpg_dual.d853))
    prob.constraints[854].save_dual_value(np.array(res.cpg_dual.d854).reshape(1))
    prob.constraints[855].save_dual_value(np.array(res.cpg_dual.d855).reshape(3))
    prob.constraints[856].save_dual_value(np.array(res.cpg_dual.d856).reshape(1))
    prob.constraints[857].save_dual_value(np.array(res.cpg_dual.d857).reshape(1))
    prob.constraints[858].save_dual_value(np.array(res.cpg_dual.d858).reshape(1))
    prob.constraints[859].save_dual_value(np.array(res.cpg_dual.d859))
    prob.constraints[860].save_dual_value(np.array(res.cpg_dual.d860).reshape(1))
    prob.constraints[861].save_dual_value(np.array(res.cpg_dual.d861).reshape(3))
    prob.constraints[862].save_dual_value(np.array(res.cpg_dual.d862).reshape(1))
    prob.constraints[863].save_dual_value(np.array(res.cpg_dual.d863).reshape(1))
    prob.constraints[864].save_dual_value(np.array(res.cpg_dual.d864).reshape(1))
    prob.constraints[865].save_dual_value(np.array(res.cpg_dual.d865))
    prob.constraints[866].save_dual_value(np.array(res.cpg_dual.d866).reshape(1))
    prob.constraints[867].save_dual_value(np.array(res.cpg_dual.d867).reshape(3))
    prob.constraints[868].save_dual_value(np.array(res.cpg_dual.d868).reshape(1))
    prob.constraints[869].save_dual_value(np.array(res.cpg_dual.d869).reshape(1))
    prob.constraints[870].save_dual_value(np.array(res.cpg_dual.d870).reshape(1))
    prob.constraints[871].save_dual_value(np.array(res.cpg_dual.d871))
    prob.constraints[872].save_dual_value(np.array(res.cpg_dual.d872).reshape(1))
    prob.constraints[873].save_dual_value(np.array(res.cpg_dual.d873).reshape(3))
    prob.constraints[874].save_dual_value(np.array(res.cpg_dual.d874).reshape(1))
    prob.constraints[875].save_dual_value(np.array(res.cpg_dual.d875).reshape(1))
    prob.constraints[876].save_dual_value(np.array(res.cpg_dual.d876).reshape(1))
    prob.constraints[877].save_dual_value(np.array(res.cpg_dual.d877))
    prob.constraints[878].save_dual_value(np.array(res.cpg_dual.d878).reshape(1))
    prob.constraints[879].save_dual_value(np.array(res.cpg_dual.d879).reshape(3))
    prob.constraints[880].save_dual_value(np.array(res.cpg_dual.d880).reshape(1))
    prob.constraints[881].save_dual_value(np.array(res.cpg_dual.d881).reshape(1))
    prob.constraints[882].save_dual_value(np.array(res.cpg_dual.d882).reshape(1))
    prob.constraints[883].save_dual_value(np.array(res.cpg_dual.d883))
    prob.constraints[884].save_dual_value(np.array(res.cpg_dual.d884).reshape(1))
    prob.constraints[885].save_dual_value(np.array(res.cpg_dual.d885).reshape(3))
    prob.constraints[886].save_dual_value(np.array(res.cpg_dual.d886).reshape(1))
    prob.constraints[887].save_dual_value(np.array(res.cpg_dual.d887).reshape(1))
    prob.constraints[888].save_dual_value(np.array(res.cpg_dual.d888).reshape(1))
    prob.constraints[889].save_dual_value(np.array(res.cpg_dual.d889))
    prob.constraints[890].save_dual_value(np.array(res.cpg_dual.d890).reshape(1))
    prob.constraints[891].save_dual_value(np.array(res.cpg_dual.d891).reshape(3))
    prob.constraints[892].save_dual_value(np.array(res.cpg_dual.d892).reshape(1))
    prob.constraints[893].save_dual_value(np.array(res.cpg_dual.d893).reshape(1))
    prob.constraints[894].save_dual_value(np.array(res.cpg_dual.d894).reshape(1))
    prob.constraints[895].save_dual_value(np.array(res.cpg_dual.d895))
    prob.constraints[896].save_dual_value(np.array(res.cpg_dual.d896).reshape(1))
    prob.constraints[897].save_dual_value(np.array(res.cpg_dual.d897).reshape(3))
    prob.constraints[898].save_dual_value(np.array(res.cpg_dual.d898).reshape(1))
    prob.constraints[899].save_dual_value(np.array(res.cpg_dual.d899).reshape(1))
    prob.constraints[900].save_dual_value(np.array(res.cpg_dual.d900).reshape(1))
    prob.constraints[901].save_dual_value(np.array(res.cpg_dual.d901))
    prob.constraints[902].save_dual_value(np.array(res.cpg_dual.d902).reshape(1))
    prob.constraints[903].save_dual_value(np.array(res.cpg_dual.d903).reshape(3))
    prob.constraints[904].save_dual_value(np.array(res.cpg_dual.d904).reshape(1))
    prob.constraints[905].save_dual_value(np.array(res.cpg_dual.d905).reshape(1))
    prob.constraints[906].save_dual_value(np.array(res.cpg_dual.d906).reshape(1))
    prob.constraints[907].save_dual_value(np.array(res.cpg_dual.d907))
    prob.constraints[908].save_dual_value(np.array(res.cpg_dual.d908).reshape(1))
    prob.constraints[909].save_dual_value(np.array(res.cpg_dual.d909).reshape(3))
    prob.constraints[910].save_dual_value(np.array(res.cpg_dual.d910).reshape(1))
    prob.constraints[911].save_dual_value(np.array(res.cpg_dual.d911).reshape(1))
    prob.constraints[912].save_dual_value(np.array(res.cpg_dual.d912).reshape(1))
    prob.constraints[913].save_dual_value(np.array(res.cpg_dual.d913))
    prob.constraints[914].save_dual_value(np.array(res.cpg_dual.d914).reshape(1))
    prob.constraints[915].save_dual_value(np.array(res.cpg_dual.d915).reshape(3))
    prob.constraints[916].save_dual_value(np.array(res.cpg_dual.d916).reshape(1))
    prob.constraints[917].save_dual_value(np.array(res.cpg_dual.d917).reshape(1))
    prob.constraints[918].save_dual_value(np.array(res.cpg_dual.d918).reshape(1))
    prob.constraints[919].save_dual_value(np.array(res.cpg_dual.d919))
    prob.constraints[920].save_dual_value(np.array(res.cpg_dual.d920).reshape(1))
    prob.constraints[921].save_dual_value(np.array(res.cpg_dual.d921).reshape(3))
    prob.constraints[922].save_dual_value(np.array(res.cpg_dual.d922).reshape(1))
    prob.constraints[923].save_dual_value(np.array(res.cpg_dual.d923).reshape(1))
    prob.constraints[924].save_dual_value(np.array(res.cpg_dual.d924).reshape(1))
    prob.constraints[925].save_dual_value(np.array(res.cpg_dual.d925))
    prob.constraints[926].save_dual_value(np.array(res.cpg_dual.d926).reshape(1))
    prob.constraints[927].save_dual_value(np.array(res.cpg_dual.d927).reshape(3))
    prob.constraints[928].save_dual_value(np.array(res.cpg_dual.d928).reshape(1))
    prob.constraints[929].save_dual_value(np.array(res.cpg_dual.d929).reshape(1))
    prob.constraints[930].save_dual_value(np.array(res.cpg_dual.d930).reshape(1))
    prob.constraints[931].save_dual_value(np.array(res.cpg_dual.d931))
    prob.constraints[932].save_dual_value(np.array(res.cpg_dual.d932).reshape(1))
    prob.constraints[933].save_dual_value(np.array(res.cpg_dual.d933).reshape(3))
    prob.constraints[934].save_dual_value(np.array(res.cpg_dual.d934).reshape(1))
    prob.constraints[935].save_dual_value(np.array(res.cpg_dual.d935).reshape(1))
    prob.constraints[936].save_dual_value(np.array(res.cpg_dual.d936).reshape(1))
    prob.constraints[937].save_dual_value(np.array(res.cpg_dual.d937))
    prob.constraints[938].save_dual_value(np.array(res.cpg_dual.d938).reshape(1))
    prob.constraints[939].save_dual_value(np.array(res.cpg_dual.d939).reshape(3))
    prob.constraints[940].save_dual_value(np.array(res.cpg_dual.d940).reshape(1))
    prob.constraints[941].save_dual_value(np.array(res.cpg_dual.d941).reshape(1))
    prob.constraints[942].save_dual_value(np.array(res.cpg_dual.d942).reshape(1))
    prob.constraints[943].save_dual_value(np.array(res.cpg_dual.d943))
    prob.constraints[944].save_dual_value(np.array(res.cpg_dual.d944).reshape(1))
    prob.constraints[945].save_dual_value(np.array(res.cpg_dual.d945).reshape(3))
    prob.constraints[946].save_dual_value(np.array(res.cpg_dual.d946).reshape(1))
    prob.constraints[947].save_dual_value(np.array(res.cpg_dual.d947).reshape(1))
    prob.constraints[948].save_dual_value(np.array(res.cpg_dual.d948).reshape(1))
    prob.constraints[949].save_dual_value(np.array(res.cpg_dual.d949))
    prob.constraints[950].save_dual_value(np.array(res.cpg_dual.d950).reshape(1))
    prob.constraints[951].save_dual_value(np.array(res.cpg_dual.d951).reshape(3))
    prob.constraints[952].save_dual_value(np.array(res.cpg_dual.d952).reshape(1))
    prob.constraints[953].save_dual_value(np.array(res.cpg_dual.d953).reshape(1))
    prob.constraints[954].save_dual_value(np.array(res.cpg_dual.d954).reshape(1))
    prob.constraints[955].save_dual_value(np.array(res.cpg_dual.d955))
    prob.constraints[956].save_dual_value(np.array(res.cpg_dual.d956).reshape(1))
    prob.constraints[957].save_dual_value(np.array(res.cpg_dual.d957).reshape(3))
    prob.constraints[958].save_dual_value(np.array(res.cpg_dual.d958).reshape(1))
    prob.constraints[959].save_dual_value(np.array(res.cpg_dual.d959).reshape(1))
    prob.constraints[960].save_dual_value(np.array(res.cpg_dual.d960).reshape(1))
    prob.constraints[961].save_dual_value(np.array(res.cpg_dual.d961))
    prob.constraints[962].save_dual_value(np.array(res.cpg_dual.d962).reshape(1))
    prob.constraints[963].save_dual_value(np.array(res.cpg_dual.d963).reshape(3))
    prob.constraints[964].save_dual_value(np.array(res.cpg_dual.d964).reshape(1))
    prob.constraints[965].save_dual_value(np.array(res.cpg_dual.d965).reshape(1))

    # store additional solver information in problem object
    prob._status = "%d (for description visit https://oxfordcontrol.github.io/ClarabelDocs/)" % res.cpg_info.status
    if abs(res.cpg_info.obj_val) == 1e30:
        prob._value = np.sign(res.cpg_info.obj_val) * np.inf
    else:
        prob._value = res.cpg_info.obj_val
    primal_vars = {var.id: var.value for var in prob.variables()}
    dual_vars = {c.id: c.dual_value for c in prob.constraints}
    solver_specific_stats = {'obj_val': res.cpg_info.obj_val,
                             'status': prob._status,
                             'iter': res.cpg_info.iter,
                             'pri_res': res.cpg_info.pri_res,
                             'dua_res': res.cpg_info.dua_res,
                             'time': res.cpg_info.time}
    attr = {'solve_time': t1 - t0, 'solver_specific_stats': solver_specific_stats, 'num_iters': res.cpg_info.iter}
    prob._solution = Solution(prob.status, prob.value, primal_vars, dual_vars, attr)
    results_dict = {'solver_specific_stats': solver_specific_stats,
                    'num_iters': res.cpg_info.iter,
                    'solve_time': t1 - t0}
    prob._solver_stats = SolverStats(results_dict, 'CLARABEL')

    return prob.value
